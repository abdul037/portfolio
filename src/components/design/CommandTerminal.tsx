// Generated by scripts/port-template.mjs from the design handoff package.
// Region: Command terminal (prototype lines 2737–2778).
// Markup is a faithful port of the signed-off design; behaviour lives in
// src/lib/useViewModel.ts. Regenerate rather than hand-editing structure.
'use client'

import { Fragment } from 'react'
import { css, arr } from '@/lib/style'
import type { ViewModel } from '@/lib/useViewModel'

export function CommandTerminal({ v }: { v: ViewModel }) {
  return (
    <>
      {v.termOpen ? (
        <>
          <div style={{ position: "fixed", top: "0", right: "0", bottom: "0", width: "300px", zIndex: "95", background: "#08080f", borderLeft: "1px solid rgba(var(--aRGB,52,211,153),0.2)", display: "flex", flexDirection: "column", animation: "modalSlide 0.3s cubic-bezier(.2,.7,.2,1)", boxShadow: "-8px 0 40px rgba(0,0,0,0.5)" }}>
            <div style={{ padding: "12px 14px", borderBottom: "1px solid rgba(var(--aRGB,52,211,153),0.12)", display: "flex", alignItems: "center", gap: "8px", flexShrink: "0" }}>
              <div style={{ width: "7px", height: "7px", borderRadius: "50%", background: "var(--a,#34d399)", animation: "pulseDot 2.4s ease-in-out infinite" }} />
              <div style={{ fontFamily: "var(--font-ibm-plex-mono), monospace", fontSize: "11px", fontWeight: "600", color: "var(--a,#34d399)", letterSpacing: "0.06em", flex: "1" }}>
                NAVIGATOR
              </div>
              <div style={{ fontFamily: "var(--font-ibm-plex-mono), monospace", fontSize: "9px", color: "#63636d", letterSpacing: "0.06em" }}>
                Ctrl+K
              </div>
              <button type="button" onClick={v.termClose} style={{ background: "none", border: "none", cursor: "pointer", color: "#63636d", fontSize: "14px", padding: "2px" }} className="hv-1">
                ✕
              </button>
            </div>
            <div data-term-log style={{ flex: "1", overflowY: "auto", padding: "12px" }}>
              <div style={{ fontFamily: "var(--font-ibm-plex-mono), monospace", fontSize: "8px", letterSpacing: "0.16em", textTransform: "uppercase", color: "#63636d", marginBottom: "10px" }}>
                {"// Walkthrough"}
              </div>
              {arr(v.termStepItems).map((ts: any, tsIndex: number) => (
                <Fragment key={tsIndex}>
                  <div onClick={ts.onClick} style={css(`cursor: pointer; padding: 10px 12px; border-radius: 8px; margin-bottom: 4px; border: 1px solid transparent; transition: all 0.2s; ${ts.style}`)} className="hv-33">
                    <div style={{ display: "flex", alignItems: "center", gap: "8px", marginBottom: "3px" }}>
                      <span style={css(`font-family: var(--font-ibm-plex-mono), monospace; font-size: 11px; font-weight: 700; ${ts.numColor}`)}>
                        {ts.num}
                      </span>
                      <span style={css(`font-size: 12px; font-weight: 600; ${ts.labelColor}`)}>
                        {ts.label}
                      </span>
                    </div>
                    <div style={{ fontSize: "10.5px", lineHeight: "1.5", color: "#555560", paddingLeft: "26px" }}>
                      {ts.note}
                    </div>
                  </div>
                </Fragment>
              ))}
              {v.termHistory.length ? (
                <>
                  <div style={{ fontFamily: "var(--font-ibm-plex-mono), monospace", fontSize: "8px", letterSpacing: "0.16em", textTransform: "uppercase", color: "#63636d", margin: "16px 0 8px" }}>
                    {"// Terminal Log"}
                  </div>
                  {arr(v.termHistory).map((th: any, thIndex: number) => (
                    <Fragment key={thIndex}>
                      <div style={{ marginBottom: "6px", fontFamily: "var(--font-ibm-plex-mono), monospace", fontSize: "11px" }}>
                        <div style={{ color: "var(--a,#34d399)" }}>
                          $ {th.cmd}
                        </div>
                        <div style={{ color: "#8f8f9b", marginTop: "1px" }}>
                          {th.res}
                        </div>
                      </div>
                    </Fragment>
                  ))}
                </>
              ) : null}
            </div>
            <div style={{ padding: "12px 16px", borderTop: "1px solid rgba(var(--aRGB,52,211,153),0.12)", flexShrink: "0" }}>
              <div style={{ display: "flex", alignItems: "center", gap: "8px", background: "rgba(255,255,255,0.02)", border: "1px solid rgba(var(--aRGB,52,211,153),0.15)", borderRadius: "8px", padding: "10px 12px" }}>
                <span style={{ fontFamily: "var(--font-ibm-plex-mono), monospace", fontSize: "13px", color: "var(--a,#34d399)", flexShrink: "0" }}>
                  $
                </span>
                <input id="term-input" type="text" placeholder="Type project name or 'help'…" onKeyDown={v.termKeyDown} style={{ flex: "1", background: "none", border: "none", outline: "none", color: "#e0e0e6", fontFamily: "var(--font-ibm-plex-mono), monospace", fontSize: "12px", caretColor: "var(--a,#34d399)" }} />
              </div>
              <div style={{ fontFamily: "var(--font-ibm-plex-mono), monospace", fontSize: "8px", color: "#3a3a42", marginTop: "6px", padding: "0 4px" }}>
                Any project name works · sections · next/prev · help for full list
              </div>
            </div>
          </div>
        </>
      ) : null}
    </>
  )
}
