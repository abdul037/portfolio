// Generated by scripts/port-template.mjs from the design handoff package.
// Region: Card hover preview (prototype lines 1775–1787).
// Markup is a faithful port of the signed-off design; behaviour lives in
// src/lib/useViewModel.ts. Regenerate rather than hand-editing structure.
'use client'

import { css } from '@/lib/style'
import type { ViewModel } from '@/lib/useViewModel'

export function HoverPreview({ v }: { v: ViewModel }) {
  return (
    <>
      {v.hoverOpen ? (
        <>
          <div style={css(`position: fixed; left: ${v.hoverX}px; top: ${v.hoverY}px; z-index: 600; width: 360px; pointer-events: none; background: #0e0e17; border: 1px solid rgba(var(--aRGB,52,211,153),0.25); border-radius: 12px; overflow: hidden; box-shadow: 0 18px 50px rgba(0,0,0,0.65);`)}>
            {v.hoverImg ? (
              <>
                <div style={css(`width: 100%; height: 150px; background-image: url('${v.hoverImg}'); background-size: cover; background-position: top center; border-bottom: 1px solid rgba(255,255,255,0.06);`)} />
              </>
            ) : null}
            <div style={{ padding: "14px 18px 16px" }}>
              <div style={{ fontFamily: "var(--font-ibm-plex-mono), monospace", fontSize: "8.5px", letterSpacing: "0.14em", textTransform: "uppercase", color: "var(--a,#34d399)" }}>
                {v.hoverBadge}
              </div>
              <div style={{ fontSize: "15px", fontWeight: "700", letterSpacing: "-0.01em", color: "#f0f0f4", margin: "5px 0 7px" }}>
                {v.hoverName}
              </div>
              <p style={{ fontSize: "12px", lineHeight: "1.6", color: "#9a9aa5", margin: "0" }}>
                {v.hoverDesc}
              </p>
              <div style={{ fontFamily: "var(--font-ibm-plex-mono), monospace", fontSize: "9px", letterSpacing: "0.08em", color: "rgba(var(--aRGB,52,211,153),0.7)", marginTop: "10px" }}>
                Click for the full case &#8594;
              </div>
            </div>
          </div>
        </>
      ) : null}
    </>
  )
}
