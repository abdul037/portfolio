// The Twitter card reuses the OpenGraph card — same 1200×630 design.
// `runtime` is declared here rather than re-exported (Next forbids that).
export const runtime = 'nodejs'
export { alt, size, contentType } from './opengraph-image'
export { default } from './opengraph-image'
